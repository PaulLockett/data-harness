# case_001 — minimal goal → structural plan

A single-line goal ("Validate the ADEM facility extraction") yields the
deterministic 5-step rule-based plan. Predicates assert structural shape only.

When `kind = "llm"` is wired (cassette replay), case_002 will exercise that
path with a recorded llm() output.
