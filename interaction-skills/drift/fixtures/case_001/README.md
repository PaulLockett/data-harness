# case_001 — planted mean shift

Two snapshots of the same schema (`x` numeric, `label` categorical):

- `a.parquet` — 200 rows, `x ~ N(10, 2)`
- `b.parquet` — 200 rows, `x ~ N(13, 2)` (mean shifted by 1.5σ)

Exercises the numeric quantile-shift branch of `drift`. With this much shift
the alarm should fire (score > 0.5). The `label` column is unchanged —
placeholder for the categorical TVD path.

Generated by `gen_fixture.py`.

Future cases: case_002 = no-drift baseline; case_003 = label-distribution
shift only; case_004 = simultaneous numeric + categorical shift.
