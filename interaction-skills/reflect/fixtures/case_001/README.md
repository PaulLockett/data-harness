# case_001 — mixed pass/fail trace

5 steps, 3 passed and 2 failed. The skill must surface both lists, count the
errors, and propose at least one next action. Predicates assert the
structural contract; the planted `error: "schema mismatch"` shows up in
common_errors.
