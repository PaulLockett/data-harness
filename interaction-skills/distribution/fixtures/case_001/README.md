# case_001 — Gaussian numeric column

A 200-row parquet with `x ~ N(10, 2)` (mean ≈ 10, std ≈ 2) plus a `label`
column for diversity. Exercises the numeric path of the `distribution` skill.

Predicates assert the histogram has exactly 10 bins of non-negative integers,
counts are in a reasonable range, and the skill output dict has all 9 expected keys.

Generated by `gen_fixture.py`.

Future cases: case_002 = uniform; case_003 = bimodal; case_004 = heavy-tailed
(power law); case_005 = degenerate (all-same value).
